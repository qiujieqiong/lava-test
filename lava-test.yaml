metadata:
    name: auto-login
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "A Test Definition To Auto Login"
    os:
        - ubuntu
    devices:
        - vm1
    scope:
        - none
    environment:
        - lava-test-shell

params:
    AUTO_LOGIN_USER: deepin

install:
    deps:
        - git
    steps:
        - "mkdir -p /etc/lightdm/lightdm.conf.d"
        - 'bash -c "echo [SeatDefaults]                  >/etc/lightdm/lightdm.conf.d/auto-login.conf"'
        - 'bash -c "echo autologin-user=$AUTO_LOGIN_USER >>/etc/lightdm/lightdm.conf.d/auto-login.conf"'
        - "useradd -m deepin"

run:
    steps:
        - "export AUTO_LOGIN_USER"
        - DEBUG=yes timeout 180 bash RunCases.sh
        - echo "test">a.txt
        - ls -ahl
        - lava-test-run-attach a.txt text/plain